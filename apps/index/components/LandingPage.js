const React = require('react');
const PropTypes = React.PropTypes;

const LandingPage = React.createClass({

  render: function() {
    return (
      <div>
        Hello world, welcome to landing page
      </div>
    );
  }

});

module.exports = LandingPage;
