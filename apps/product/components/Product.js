const React = require('react');
const PropTypes = React.PropTypes;

const Product = React.createClass({

  render: function() {
    return (
      <div>
        Hello, im a product page
      </div>
    );
  }

});

module.exports = Product;
